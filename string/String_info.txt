string is a collection of characters and also string is a class
string is immutable..i.e changes cannot be done in the string once created
example String a="Soham"

since string is a class we can create an object of string
example String b=new String ("SBPCOE")

also string could be created...first creating an array then calling the same array using an object
example char [] c= {'a','p','p','l','e'}

difference between .equals() & == is
.equals() function checks the content of the two string to be compared
whereas the == checks the memory location of two string in scp 
example   String a ="Swamini";
          String b="Swamini";
          String c=new String ("Swamini");
          String e="Bhandare";
          String f=new String("Bhandare");
          System.out.println(a==b);
      		System.out.println(a.equals(b));//this both will return same output as boths content and memory location is same in scp
      		System.out.println(a==c);
      		System.out.println(a.equals(c));//this both will return different o/p as memory loaction is different yet content are same
	

